pipeline {
    agent any	 

       stages {
	stage('Running the Program') {
            steps {
                sh 'python3 Pyramid.py'
            }
        }	    
    }

    post {
	always {
	    archiveArtifacts artifacts: '**/target/*.jar', allowEmptyArchive: true
        }
    }
}
